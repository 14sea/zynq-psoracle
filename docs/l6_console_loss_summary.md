# L6 console byte-loss summary (design review 2026-09-01, item 2)

Generated by `host/l6_loss_stats.py` from the three C1 evidence directories; numbers in
`evidence/l6_console_loss_stats.json`. **Three loss events across C1 #1 and C1 #3; C1 #2
read nothing and contributes no bytes.** With three events, a position is not a pattern:
two of three landing on chunk 3 is NOT read as a chunk-3 fault.

## Denominators

| | C1 #1 (`-06`) | C1 #2 (`-07`) | C1 #3 (`-08`) | total |
|---|---|---|---|---|
| bytes received on the console | 622,135 | 0 | 1,709,326 | 2,331,461 |
| `AUDIT` frames received | 191 | 0 | 528 | 719 |
| complete (~3 KB) audit lines | 167 | 0 | 460 | 627 |
| audit words decoded | 66,768 | 0 | 184,956 | 251,724 |
| of which zero | 64,348 | — | 178,280 | 242,628 (**96.4 %**) |
| loss events | 1 | 0 | 2 | **3** |

Rates: **0.48 % of complete audit lines** carried a loss;
**1.29 events per MB** received. No loss was ever seen outside an audit line
(the other 1,000+ frames per session — HB, SIGNREQ, REC, IDENT, CLOSE, TERM — are all short).

## The events

| session | seq | chunk | shape | bytes missing |
|---|---|---|---|---|
| C1 #1 | 24 | 4 → 5 | a loss spanning the line boundary: chunk 4's tail and chunk 5's head merged into one 6002-byte line (7 fields → `FrameError` → collector `CRASHED`) | ≈ 39 |
| C1 #3 | 20 | 3 | a contiguous run missing inside the payload; the line kept its shape, failed CRC, was dropped | 309 |
| C1 #3 | 62 | 3 | same shape | 229 |

Common to all three: each fell inside a complete ~3 KB audit line, during the audit burst
(8 lines back-to-back, ~24 KB, ~1.85 s at 115 200 baud); each is a contiguous deletion,
not a bit flip; CRC caught the two that stayed inside a line, and the line-boundary one was
caught as a malformed frame. The path is CH340 → usbipd → WSL `vhci_hcd`, no flow control.

## What the zero fraction means for the design

96.4 % of the audited words are zero: the twelve target frames are
mostly zero words and the staging streams carry them plus a fixed command envelope. A
lossless sparse encoding of the same 2814 words (§3 of the review: unlisted positions are
zero, listed positions strictly ascending and unique) removes about 96 % of the audit
bytes without weakening the audit — the host still reconstructs all 2814 words and
recomputes the three hashes — and with it about 96 % of the link time the loss events
occurred in.
