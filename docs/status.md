# Canonical status — zynq-psoracle (P3)

**This table is the only authoritative statement of rung status.** Every other document
that mentions a rung's state (architecture, decisions, designs, review packages, findings)
is a record of what was true *when it was written*; where the two disagree, this table wins
and the other text is historical. Updated with every adjudication.

| rung | state | adjudicated | evidence / record |
|---|---|---|---|
| L0 host-only architecture | **PASS** | owner, whole-line gate review 2026-08-29 (`docs/whole_line_gate_review_result.md`) | `docs/p3_architecture.md` §3 v0.3, `docs/l0_review_result.md`, `validators/`, `docs/import_manifest.md` |
| D4 key custody | **PASS** | owner re-review 2026-08-29 | `evidence/boundary/` (R1–R5 as the runner), `docs/decisions.md` D4 v0.3 |
| L1 P3 carrier | **PASS** (preparation) | owner re-review 2026-08-29 | `builds/p3/` (public build, +7.58 ns, isolation 6/0, ICAPE2 0), `docs/l1_design.md` |
| L2 = P2b non-perturbation | **PASS** on 17A6 | owner 2026-08-30, run #3 (ruling `2026-08-30-03`) | `evidence/l2_17A6_2026-08-30-03/`, `docs/l2_findings.md`; heartbeat pinned [49.5, 50.5] MHz |
| L3 one gated candidate | **PASS (scoped)** on 17A6 | owner 2026-08-31, five sessions | `evidence/l3_17A6_2026-08-30-03/`, `…2026-08-31-0{2,3,4,5}/`, `docs/l3_findings.md` |
| L4 fault / restore / baseline | **PASS** on 17A6 | owner 2026-08-31 (this table records the adjudication the owner gave in the L0–L4 review; see below) | `evidence/l4_17A6_2026-08-31-06/`, `docs/l4_findings.md` |
| L0–L4 overall review | **PASS (scoped)** — 2026-08-31, after two HOLDs (documentation drift → this table + historical banners; test-evidence plumbing → fail-closed `host/test_report.py`) | owner | this file; `evidence/tests/test_report_2026-08-31T15350{1,7}Z.json` |
| L5 the loop | **PASS (scoped)** on 17A6 — session 4 (`evidence/l5_17A6_2026-09-01-04/`, image `a7c73d1f…`, spec `docs/l5_session4_spec.md`). **Scope, verbatim from the ruling:** EBAZ4203 17A6, carrier `956379fa…`, application image `a7c73d1f…`, the U-Boot→standalone control-plane crossing, a host-supplied seed, N = 8, all-self-reporting audit, under the established notary/interlock. Not extrapolated to autonomous discovery, long-run stability, other carriers/dies, Linux, or a precise ARM-gate time; `CLK_621_TRUE` unread does not affect this PASS; 16 is a count of Strongly-Ordered reads, not a time unit. **What held:** `COMPLETED` with all three closing steps done; 10/10 `SCORED`; both baselines `[18, 22, 20, 20, 20, 18]`; 80 audit chunks → 10/10 audited records, recomputed by the host gate and independently; validator `{scored 10, audited 10, chain_length 11}`; audit policy seq 1–10, no exempt; closing unsigned control `fault 13`, `cfg_valid_hw` clear; zero disruptions / CRC drops / re-reads; every ARM `status_first 0x901` → 16 reads → `status_last 0xf54`, nonce stepped once each. **The early-read explanation of sessions 1/3 is silicon-verified on the standalone plane.** No §3 falsifier. History: session 1 (`d3828a8c…`) stopped at the ARM on an immediate nonce read; session 2 (`8390c463…`, DEFECTIVE) crashed on my `CTRL` read; session 3 (`10044abe…`) captured `gate_busy` at the immediate read, adjudicated HOLD (instrument); the design correction (settle poll, tally, audit gate, Falsified/HOLD boundary) passed four review rounds. `-04` rulings consumed; board untouched since. | owner, 2026-09-01, after checking the evidence item by item against `docs/l5_prereg.md` §5 (all eight) and §3 (none) — `docs/l5_session4_findings.md` §3 | `docs/l5_session4_findings.md`, `docs/l5_session4_spec.md`, `docs/l5_settle_correction.md`, `docs/l5_prereg.md` |
| L6 calibration + soak | **§4 instrument batch PASS; §2 image `bd1454cd…` compatibility review PASS; prereg v0.2 frozen; board phase opened — C1 #1 (ruling `2026-09-01-06`) = HOLD (transport): 23 SCORED records: one opening baseline plus 22 scheduled random-safe candidates, then one byte loss on the console; No image defect was observed in the completed prefix before the transport HOLD; **C1 #2 (ruling `2026-09-01-07`) = HOLD (instrument, host): the collector's silence clock started before the four-minute preamble and, once the reader no longer blocked, ended the epoch 0.4 s after `go` with nothing read — fixed (`docs/l6_c1_session2_findings.md`)**; **C1 #3 (ruling `2026-09-01-08`) = HOLD (transport): the board COMPLETED the whole session — 66 SCORED records (1 + 64 + 1), baselines exact, closing control refused — but the console lost bytes twice inside audit bursts (308 and 228 bytes, both chunk 3) and the audit record is incomplete (`docs/l6_c1_session3_findings.md`). C1 #1's byte loss recurred: STOP-LOSS MET on the owner's standing byte-loss ruling (not the §7 three-without-COMPLETED clause — C1 #3's device end was COMPLETED) — board repeats stopped; no calibration pinned; a full transport / protocol design review precedes any fourth board session.** Three loss events across C1 #1 and C1 #3; C1 #2 read nothing. Informational: 1586 evals/h, CoV 0.019, audit stream 76 % of the period. Design-review batch (host-only, 2026-09-01): the timeline is now the one inbound ledger and CRC authority for every frame type (`host/l6_console.py`; C1 #3 counterfactual: 2 drops within budget, log still refused); loss statistics `docs/l6_console_loss_summary.md` (3 events / 627 complete audit lines / 2.33 MB; 96.4 % of audit words zero); the host-paced sparse audit pull is designed and modelled (`docs/l6_audit_pull_design.md`: 19 % of the bytes, C1 #1/#3 losses recovered by one retry, exhaustion HOLD, wrong content Falsified). Pull batch delivered host-only (`docs/l6_pull_batch_package.md`): denominators corrected (626/630), ≈80 % byte cut on the 64-audit corpus, one-transaction binding, the real two-sided wire state machine, the firmware with the bounded pull and STOP_AUDIT (no ARM after a failed pull), the C wire twin bit-identical, host/validator support, mutation tests, two byte-identical builds → `next_image e19e1b12…` (**NOT board-ready**), prereg v0.3 DRAFT (not frozen). Pull-batch package review **PASS** (owner, host-only scoped; six integration findings + two termination edges corrected on the way) and pushed (`3495b8a`). **FREEZE BATCH 2026-09-01: prereg v0.3 frozen (the draft's deltas merged into the standalone text; v0.2 superseded, in history), `e19e1b12…` promoted to the board-ready pull-v2 pin (bd1454cd… superseded, NOT defective — its C1 evidence stands under v0.2), one image one authority (next_image removed), runner binds v0.3 + pull-v2 + board_ready.** **C1 #4 (ruling `2026-09-01-09`, prereg v0.3, image `e19e1b12…`, pull-v2) = PASS (runner outcome, 2026-09-01 20:47): power cycle, boundary PASS, 66 SCORED records (1 + 64 + 1), both baselines `[18, 22, 20, 20, 20, 18]`, closing control refused, TERM COMPLETED; 66/66 audits served over the sparse pull and verified (stream/staged/readback hashes all equal), 1785 inbound frames exactly as expected, 0 CRC drops of budget 8, 0 rereads, validator accepted; rate report `786dc3ec…` = 3909.9 evals/h, CoV 0.0159, audit 47 % of a 0.92 s period (`docs/l6_c1_session4_findings.md`). All 66 genomes/readouts equal C1 #3's under `bd1454cd…` — transport-only change confirmed on hardware. **OWNER 2026-09-01: C1 #4 = PASS adjudicated after independent re-check; `calibration.C1` PINNED = `786dc3ec…` (`manifests/l6_manifest.json`); the byte-loss stop-loss is LIFTED for pull-v2 (history kept; future faults re-counted under frozen §7); C2 may proceed under its own ruling pair bound to the post-pin manifest hash, then power cycle. Cross-image identity claim is limited to these 66 genomes/readouts — not extrapolated to all inputs.** **C2 #1 (ruling `2026-09-01-10`, bound to the post-pin manifest `d84a770a…`) = PASS (runner outcome, 2026-09-01 21:06): power cycle (UART 21:00:54), boundary PASS, `map_guided_forced` under the shared seed, 66 SCORED (1 + 64 + 1), baselines `[18, 22, 20, 20, 20, 18]`, closing control refused, TERM COMPLETED; 66/66 audits pulled and verified, 1785 inbound frames exact, 0 CRC drops of 8, 0 rereads; rate report `a13e301f…` = 3633.0 evals/h, CoV 0.0150 (`docs/l6_c2_session1_findings.md`). **OWNER 2026-09-01: C2 #1 = PASS adjudicated after independent re-check; `calibration.C2` PINNED = `a13e301f…`. Both calibrations pinned. S parameters derived from the two pins and owner-verified: abba, seed 1278628687, N 6539, sampled audits 412, expected inbound frames 121449, CRC budget 486, duration 7200 s, runner timeout 8702 s. S awaits its own ruling pair bound to the post-pin manifest hash + power cycle; S is not a Claim B data run; a non-PASS S is not re-run without a ruling.** **S #1 (ruling `2026-09-01-11`, bound to `bb63b31d…`) = HOLD (transport, 2026-09-01 21:21): power cycle (UART 21:13:19), boundary PASS, S parameters derived from the two pins = the owner's (N 6539, 412 audits, 121 449 frames, budget 486, 8702 s); 464 SCORED records in 231 s, 31/31 due sampled audits pulled and verified, 0 rereads — then REC 465 arrived with ~536 contiguous bytes missing, was CRC-dropped (1 of 486) and the collector ended the epoch `CRASHED: record seq gap: 466 after 464` (§6 item 4: a missing REC is fatal regardless of budget). The C1 #1/#3 byte-loss family, first time on a REC line, which pull-v2 cannot re-request. Second finding: the collector's crash-path summary says 0 audited where the host verified 31 (validator (ix)) — a host defect to fix before the next S. No host USB event at the fault time. Archived, NOT re-run (owner's ruling); §7 count: S failure #1 (`docs/l6_s_session1_findings.md`, §5 options: REC re-request, crash-path summary fix, loss statistics extension).** **REC-TRANSACTION BATCH (rec-v3) 2026-09-01, host-only, owner-authorised (firmware/image/prereg-draft in scope; no board, no ruling, no freeze, no S): the loop record is a transaction (REC → RECACK | RECGET → same bytes ×≤3, STOP_REC on exhaustion, current-candidate authority and idempotence closed), one ledger + per-seq REC ledger, content the validator's, crash-path `audited` from the host audit gate (S #1 counterfactual: 31 → validator accepts, structural gate names missing REC/TERM, HOLD not PASS), loss statistics over all six sessions (4 events, exposure only), the preregistered forced REC-retry control (flags.bit4, seq 1) armed in every session, rate-report `binding` + S-runner refusal of unbound/other-bound calibrations (the v0.3 C1/C2 pins are historical: C1/C2 re-run under rec-v3 before any S), prereg v0.4 DRAFT (v0.3 frozen, untouched), the rec-v3 image built twice byte-identical → `next_image` `cd8360dc…` NOT board-ready, never run; the runner refuses v0.3/pull-v2 by construction. Package `docs/l6_rec_batch_package.md`, design `docs/l6_rec_transaction_design.md`.** **OWNER REVIEW 2026-09-02: HOLD on four blockers (first-byte-only receive bound; header-only re-ACK of a broken resend; PASS condition 7 unenforced; control/stale-ack/stale-limit gaps) — CORRECTION BATCH delivered host+firmware: `p3_rectx_recv_line` whole-line bound (host-driven with truncated input: resend / STOP_REC, never a block), broken resend of an accepted record → RECGET and equality-checked re-ACK, `rec_closure_findings` (PASS 7, machine-enforced, discrimination-tested), exact control shape, stale acks bound to the previous transaction, stale limit exact; `cd8360dc…` withdrawn DEFECTIVE; `403f4ab5…` built twice byte-identical = `next_image`, NOT board-ready, never run. 863 tests / 1 skip. OWNER RE-REVIEW 2026-09-02: correction batch PASS (all four blockers substantively closed, no new blocker; artifact boundary confirmed); then FULL P3 COMPATIBILITY REVIEW 2026-09-02 = PASS (host-only, scoped; not a board/L6 session PASS): push approved and done; PROMOTION/FREEZE BATCH done — `403f4ab5…` the sole board-ready pin (rec-v3), `e19e1b12…` superseded NOT defective with its sessions' evidence and calibrations kept as history, `cd8360dc…` DEFECTIVE, **prereg v0.4 FROZEN** (v0.3 superseded, in history), `calibration.C1/C2` null under rec-v3 (pull-v2 pins historical, refused for S by test). FROZEN-ARTIFACT/HASH SHORT REVIEW 2026-09-02 = PASS (hashes prereg `12799ef9…`, manifest `f12b6958…`, image `403f4ab5…`, ELF `8687ef8d…` confirmed; preflight with the real pins yields a C1/rec-v3/N=64/control-armed plan); `cb072eb` pushed. No ruling issued, no board. Next: the rec-v3 C1 ruling pair (bound to the committed manifest sha) + power cycle → C2 → owner pins the bound records → S.** **C1 #5 (ruling `2026-09-02-01`, prereg v0.4 `12799ef9…`, image `403f4ab5…`, rec-v3, bound to manifest `f12b6958…`) = HOLD (instrument, §6 item 3; runner outcome 2026-09-02 18:10): power cycle (UART 18:04:26), boundary PASS, first hardware run of the rec-v3 image; 66 SCORED (1 + 64 + 1), baselines `[18, 22, 20, 20, 20, 18]`, closing control refused, TERM COMPLETED; IDENT `rec-v3` + control echoed; the forced REC-retry control on seq 1 exactly `[crc, ok]` (the session's one CRC drop of budget 8); REC closure clean; 66/66 audits pulled and verified; 1785 inbound frames exact. One transport event: AUDIT 39 chunk 1 arrived with bytes missing and no terminator, the pull timed out (≈2.09 s), the resend merged into a `BAD_FRAME`, the third request was clean — recovered, epoch completed, but that one period (3.095 s of 63, median 0.965 s) makes CoV 0.274 > 0.10 (0.056 without it, informational). Rate 3607.8 evals/h informational; nothing pinned; archived, NOT re-run (the owner's authorisation covered one C1 epoch). Fifth console byte-loss event (C1 #1, C1 #3 ×2, S #1, C1 #5), no host USB event at the fault; §7 classification and any §6 item 3 revision are the owner's (`docs/l6_c1_session5_findings.md` §5).** **OWNER 2026-09-02: C1 #5 HOLD correct, not pinnable; `3e32f7a` pushed; STOP-LOSS TRIGGERED (S #1 + C1 #5 = two consecutive sessions lost to the same transport failure class; a classification, not a proven common root cause) — no board ruling, C2/S/Claim B paused; v0.5 host-only design batch authorised (C1 #5 stays HOLD under v0.4, `calibration.C1` null; inclusive rate for N + nominal CoV with a preregistered minimum clean sample + independent recovery bounds; C1/C2 re-run under v0.5); the reader-residue defect (`host/l6_reader.py` kept an unterminated fragment for ever, the resend glued to it) to be fixed host-only with quarantine, resync, a monotonic deadline, a C1 #5 byte replay, discrimination tests and a host soak — the timeout not merely re-pinned (≈0.5 s a candidate; C1 #5 round trips median 42.9 ms / p99 83.1 / max 83.6). TRANSPORT BATCH delivered host-only the same day (`docs/l6_transport_batch_package.md`, NOT reviewed): reader resync + fragment quarantine (FRAGMENT ledger events), monotonic chunk deadline, stale duplicates ignored, runner wiring, rate report 1.1.0 (inclusive / nominal / recovery; C1 #5 = 0.274 / 0.056 over 62, seq 39 excluded), `calibration_findings_v05` selected only under a v0.5 manifest, prereg v0.5 DRAFT (`docs/l6_soak_prereg_v0.5_draft.md`) with draft bounds in the manifest's `next_prereg`; C1 #5's bytes replayed — the first resend recovers (`[timeout, ok]`; glued `[ok]`), the old reader reproduces `[timeout, malformed, ok]`; soak 2000 candidates × 5 configurations — every single fault recovered on the first resend at 2.0 s and 0.5 s, the C1 #5 reader 1466/2132 with 1530 bad frames. `CHUNK_TIMEOUT_S` stays 2.0 s. Next: owner review of the package → D-t1..D-t3 → v0.5 freeze → stop-loss ruling → only then a C1 ruling pair bound to v0.5.** **OWNER 2026-09-02 (transport batch): PASS host transport fix (scoped) / HOLD v0.5 freeze + return to the board; `b58d0fd` pushed; D-t3 PINNED 2.0 s (0.5 s not adopted); D-t1/D-t2 accepted in principle; three freeze blockers (half-ledger reports; run-log-only hash binding; the inclusive rate blind to a last-candidate recovery) + a self-contained text required; stop-loss NOT lifted (the soak proves the model, not the path; SIGNREQ/HB/AUDIT_READY/CLOSE/TERM still not re-requestable); no C1 ruling; C2/S/Claim B paused; firmware/protocol/image changes ruled after a reliability design review. CORRECTION BATCH delivered host-only (`docs/l6_transport_batch_package.md` §8, NOT reviewed): both ledgers or neither (coverage-checked, CLI refuses a half set), `inputs` = the three files' sha256 as written + `calibration_inputs_findings` at the S import, the planning rate (bracketed span; C1 #5 3508.9 < 3607.8 < 3734.4) sizing S under v0.5 with the last-candidate counterexample test, rate report 1.2.0, the v0.5 draft rewritten self-contained (`docs/l6_soak_prereg_v0.5_draft.md`), `docs/l6_frame_reliability_design.md` (S exposure ≈7.5 fatal-class events per soak under the per-byte reading; per-frame re-request/reconstruct/budget proposals; host-only parts first, one firmware batch → v0.6). Next: owner review of §8 + the design → D-t1/D-t2 confirmation → v0.5 freeze or hold for v0.6 → stop-loss ruling.** **OWNER 2026-09-02 (correction batch): PASS the correction line / HOLD the design (4 items); `62d2922` pushed; D-t1 accepted; D-t2 accepted with two fail-closed fixes (duplicate ledgers; the report computed from the files on disk); v0.5 NOT frozen — straight to v0.6 (rec-v3 cannot survive a long soak; no executable intermediate); stop-loss stays TRIGGERED; the rel-v4 host batch authorised, the firmware batch opened but gated on its review. REL-V4 HOST BATCH delivered host-only (`docs/l6_rel_batch_package.md`, NOT reviewed): D-t2 fixes; `host/l6_rel.py` (IDENT handshake before the first SIGNREQ, SIGNREQ transaction with one signature + cached reply, AUDITREQ in SIGNOK, READY resend, AUDITDONE handshake with `unconfirmed`, indexed HB with budget ⌊R/1000⌋, CLOSE from TERM, TERM transaction) behind `ConsoleSession(protocol="rel-v4")`, rec-v3 unchanged by test; validator STOP_SIGN contract + rule (vii-b); 38 tests; design rev. 2 (four items closed, no probability); `docs/l6_soak_prereg_v0.6_draft.md` self-contained, v0.5 draft superseded never frozen. Next: owner review of the rel-v4 host batch + design rev. 2 → firmware batch (new image, full P3 compatibility review) → v0.6 freeze → stop-loss ruling → ONE C1 → C2 → S.** **OWNER 2026-09-02 (rel-v4 host batch): HOLD on seven integration items (stale AUDITWAIT ledger copy; premature `unconfirmed`; the TERM re-ack unreachable in the real runner; IDENT wiring — CRC-bad not ledgered, malformed CRASHED, refusal ending the epoch instead of the board's STOP_IDENT; §6.10–13 not enforced; STOP_SIGN accepted under rec-v3; CLOSE/TERM not compared); D-p1 accepted (3 transmissions, GET ≤ 2, WAIT ≤ 3, replay ≤ 3; the third WAIT is not a final failure); `866bc5b` NOT pushed; firmware batch not yet authorised. CORRECTION BATCH delivered host-only (`docs/l6_rel_batch_package.md` §8, NOT reviewed): live pull ledgers, the verdict from the board's record via `rel_closure_findings`, `session_loop_continues` lingering 22 s after the first TERM, IDENT broken lines ledgered + refusal without a host-side end, the §6.10–13 gates as code with the rel recovery indicators and `FLAG_SIGN_CONTROL`, STOP_SIGN gated to rel-v4, CLOSE/TERM compared; `tests/test_l6_rel_correction.py` (14) on the real session; design rev. 3; v0.6 draft updated. Next: owner review of §8 + rev. 3 → push → firmware batch → v0.6 freeze → stop-loss ruling → ONE C1 → C2 → S.** **OWNER 2026-09-02 (second review): main paths of the seven confirmed; HOLD on two acceptance blockers (sign ledgers last-wins; TERM without closing_control passing beside a CLOSE), two pins before firmware (bit5 IDENT echo; the 22 s linger vs the C poll bound), two minor (refused-repeat bytes; the HB 99.9 % wording); D-p1 accepted; no push, no firmware. SECOND CORRECTION BATCH delivered host-only (`docs/l6_rel_batch_package.md` §10, NOT reviewed): `unique_ledgers_by_seq` (never last-wins, seq sets equal, both orders tested), mandatory typed `closing_control` (four cases on the real session), IDENT 1.3.0 `sign_retry_control` verified at both checks, `BOARD_BOUND_WALL_MAX_S` + `FIRMWARE_BOUND_CONTRACT` with the linger derived from it, refused-repeat compares bytes, HB docstring; `tests/test_l6_rel_correction2.py` (11). Next: owner short review of §10 → push (866bc5b, e2c0caf, this) → firmware batch (bound contract + IDENT 1.3.0 as deliverables) → v0.6 freeze → stop-loss ruling → ONE C1 → C2 → S.** **OWNER 2026-09-02 (third review): the six confirmed; HOLD on one narrow gap — the rate accepted a rel-v4 report with `audits.signs` missing or null (sign_retries 0). CLOSED host-only (`docs/l6_rel_batch_package.md` §12): `_check_ledgers` protocol-aware — rel-v4 requires signs/ident/term keys (signs a list, null refused), non-integer seqs are RateErrors, rec-v3 unchanged; tests through the real `rate_report_from_evidence_dir()` on a C1 #5 copy (deleted / null / missing one / duplicate / bad seq refused; rec-v3 passes). Next: owner's shortest review of §12 → push (866bc5b, e2c0caf, e61e994, this) → firmware batch (IDENT 1.3.0 echo + board-bound contract proof mandatory) → v0.6 freeze → stop-loss ruling → ONE C1 → C2 → S.** **OWNER 2026-09-03: §12 PASS; the four commits pushed (origin a8c950d); the firmware batch opened with mandatory deliverables. FIRMWARE BATCH delivered (`docs/l6_rel_firmware_package.md`, NOT reviewed): the rel-v4 image — `p3_tx_run` transactions for IDENT/SIGNREQ/TERM with the strict previous-ack rule, the timed receiver bounded by polls AND global-timer ticks (P3_BOUND_S 8 s on the pinned 333,333,343 Hz timer, source-derived proof ≤ the host's 10 s), `p3_pull.c` (READY resend, AUDITWAIT), STOP_IDENT/STOP_SIGN, indexed HB, closing_control in the TERM, IDENT 1.3.0 bit5 echo, no AUDITREQ frame; the C twins driven by the real host objects (17) + the source audit (12); two from-scratch builds byte-identical → `next_image` `734d6c04…` (ELF `a2a42215…`, 98 324 B), `board_ready: false`, NEVER run. Next: the owner's full P3 compatibility review of the firmware package → promotion → v0.6 freeze → stop-loss ruling → ONE C1 → C2 → S.** Historical: awaited the frozen-artifact short review, then the owner's manifest-bound ruling pair and power-cycle sequence (issued as `2026-09-01-09`). History: §2 delivered host-only; first compatibility review HOLD (image `47b8fa09…` withdrawn as DEFECTIVE — watchdog kicked before initialisation; build evidence cited a stale report) → corrected the same day: image **`bd1454cd…`** built and pinned (two from-scratch builds byte-identical), evidence cites a named green report; compatibility review **PASS (owner, host-only, scoped)** and pushed (`3c349c3`); **preregistration v0.2 FROZEN 2026-09-01** (`prereg.sha256` = `90f5fa69…`, freeze-time guard: the build evidence must cite a green report) — board-phase preflight: five blockers closed host-only, **closing review HOLD (2026-09-01) on three more — the manifest itself unbound by the rulings, `getpass` identity forgeable by environment, the nonce seed from an unbound L5 manifest — corrected host-only (rulings bind `l6_manifest_sha256`; identity = the effective UID's name; nonce seed pinned in the L6 manifest, `--l5-manifest` removed), closing re-review PASS, pushed (`183b136`)**; **board phase opened: C1 #1 (2026-09-01-06) = HOLD** — 23 SCORED records: one opening baseline plus 22 scheduled random-safe candidates under the watchdog-ON two-operator image, then one contiguous ~38-byte loss on the console inside seq 24's audit burst → `CRASHED: unparseable frame`, validator transport-class rejection (`docs/l6_c1_session1_findings.md`); staged batch stopped, no calibration pin; second finding: per-frame timestamps are per-burst (`drain()` blocks until 100 ms of silence) so the breakdown is void on hardware while the rate (≈1470 evals/h, CoV 0.019) is sound; then a staged batch C1 → C2 → S, each with its own bound rulings.** Prereg v0.2 reviewed and pushed (`04d09ea`); §4 delivered (`451f8b2`), re-review HOLD × 2 corrected (`1741c53`: heartbeat over HB frames only, STOP_AXI exempt by content, steady-state periods, mutation_bits frozen as the operator contract, single-sample CoV None; `6ee3c38`: exactly 16 HB per SCORED record in the shared structural gate) → PASS. Owner's independent re-run 571 OK (one pre-existing principal-boundary test skipped in the review environment; the fail-closed report here is 571/0 skip — not a contradiction). §2 scope: A/B operators + A,B,B,A/forced schedule + pair-seed C twin; mutation_bits = 4 and operator_data_sha256 contract; IDENT 1.1, candidate arm, flags bits 2–3; sampled audit with auto-audit of every non-SCORED self-report incl. post-staging STOP_AXI; watchdog ON (prescaler 7, load 1 250 000 035, flag-gated); 16 HB per SCORED record in seq/order; 256-corpus bit-exact twin; real C wire→relay→validator contract tests; two clean byte-identical rebuilds, build evidence, manifest/image hash, P3 compatibility review package. §2 delivered: `firmware/p3_search.c` (two operators + A,B,B,A schedule, C twin bit-exact on the 256-pair corpus), `p3_data.h` map tables hashed to the pinned derivation, wire 1.1 (IDENT fields, record `arm`), §3a auto-audit incl. post-staging STOP_AXI, watchdog ON (prescaler 7, load 1 250 000 035, WD mode, flag-gated), 16 HB per SCORED record pinned structurally, real C wire→relay→validator contract tests, `manifests/l6_bsp_inputs.json` (same 65 files as L5), `evidence/l6_build/`. Latent L5 defect found and fixed (link-2 refusal could never serve its words; no session took that path). Next: host-only instrument batch (non-blocking per-read timestamps; C1 #1's breakdown void) → short review → new bound rulings → C1 #2 (power cycle); a second byte loss of the same kind stops board repeats (stop-loss) and moves to transport/protocol design. | owner, 2026-09-01 (§4 PASS); C1 #4 / C2 #1 PASS adjudicated + pull-v2 calibrations pinned 2026-09-01 (now historical); S #1 HOLD; rec-v3 batch full P3 compatibility review PASS + promotion + v0.4 freeze, owner 2026-09-02; C1 #5 HOLD + §7 stop-loss TRIGGERED, owner 2026-09-02; rel-v4 host batch reviewed PASS + pushed (a8c950d), firmware batch opened, owner 2026-09-02; rel-v4 firmware package full P3 compatibility review HOLD (early AUDITDONE accepted; whole-line bound 32 s; AUDITWAIT.served counted transmissions) → `734d6c04…` withdrawn DEFECTIVE, corrected image `5deee74c…` as `next_image` (`board_ready: false`), short re-review: three fixes PASS, HOLD on an evidence-closure blocker (archived build records pointed at live paths) → all five archived records closed (archived maps, hash-only binaries, `BuildEvidenceClosure`), evidence-closure review PASS + push (`origin/main` = `57cc22b`), owner 2026-09-03; **PROMOTION/FREEZE BATCH 2026-09-03 (owner-authorised, host-only, not a board authorisation): `5deee74c…` (ELF `ebe97ce6…`, 98 324 B) the sole `pinned_at_build` (protocol rel-v4, `board_ready: true`, firmware bytes unchanged, no rebuild); `403f4ab5…` superseded NOT defective (its C1 #5 HOLD kept under v0.4; record archived as `build_evidence_403f4ab5.json`); `734d6c04…` stays withdrawn DEFECTIVE; the candidate entry removed (one image, one authority); prereg v0.6 FROZEN (`docs/l6_soak_prereg.md`, protocol rel-v4, sha pinned; v0.4 `12799ef9…` superseded in history unchanged; v0.5 never frozen; the draft's stale present tense — design revision 2, firmware batch not started, candidate/board_ready false, pending re-review — corrected and guarded); the draft bounds merged into `pass_conditions`; calibration.C1/C2 null, pull-v2 pins historical and refused, no rec-v3 pin ever existed. Stop-loss stays TRIGGERED through the batch; a conditional lifting only after the frozen-artifact short review and opening at most ONE rel-v4 C1 — C2/S/Claim B not opened; C1 #5 permanent HOLD, not re-judged. No ruling, no board contact, not pushed.** **Frozen-artifact short review PASS + `d836107` pushed (`origin/main`), stop-loss CONDITIONALLY LIFTED for ONE rel-v4 C1 (owner 2026-09-03). C1 #6 (ruling `2026-09-03-01`, prereg v0.6 `bfd69d10…`, image `5deee74c…`, manifest `1746cdfa…`, rel-v4) = PASS (runner outcome, 2026-09-03 15:53): power cycle (UART 15:46), boundary PASS, IDENT 1.3.0 rel-v4 with both controls echoed and IDENTACKed once, 66 SCORED (1 + 64 + 1), baselines exact, closing control fault 13 in TERM and CLOSE agreeing, TERM COMPLETED; 66/66 audits pulled and verified with zero retries/waits, 1785 inbound frames exact, CRC drops 2/8 = the two seq-1 controls (`[crc, ok]` each), 0 bad frames, 0 fragments, 0 recoveries; rate report `08222f85…` = 3495.7 evals/h inclusive, CoV 0.0151, planning 3381.4 evals/h (`docs/l6_c1_session6_findings.md`). Owner adjudication and the `calibration.C1` pin pending; C2/S/Claim B not opened; C1 #5 stays HOLD.** **OWNER 2026-09-03: C1 #6 = PASS adjudicated after independent re-check (validator 66/66/67; 66 × 3 audit hashes recomputed; rate report regenerated field-identical; inputs hash); `calibration.C1` PINNED = `08222f85…` (`9c6772d` pushed; the pin commit changes the manifest hash — the C2 ruling pair binds the new committed sha256). Stop-loss operational exception advanced to exactly ONE rel-v4 C2, to be ruled only after the pin commit is pushed; a non-PASS C2 not re-run; S and Claim B closed. Scope of the PASS: a normal complete epoch plus the two forced SIGNREQ/REC retry controls on the board; the 8 s timeout, READY resend, AUDITWAIT and TERMGET paths were not exercised.** **Manifest narrative drift fixed (`23477a7` pushed, manifest `154a7525…`); C2 ruling pair `2026-09-03-02` issued by the owner bound to it. C2 #2 (prereg v0.6, image `5deee74c…`, rel-v4, `map_guided_forced`, same seed as C1) = PASS (runner outcome, 2026-09-03 17:53): power cycle (UART 17:47), boundary PASS, IDENT 1.3.0 with both controls echoed and IDENTACKed once, 66 SCORED (1 + 64 + 1), baselines exact, closing control fault 13 (TERM and CLOSE agree), TERM COMPLETED; 66/66 audits verified; 1785 inbound frames exact; CRC drops 4/8 = the two seq-1 controls (`[crc, ok]` each) + two AUDIT chunks (seq 35 chunk 7, seq 50 chunk 5) that arrived CRC-broken and were re-requested once each with no timeout (≈42 ms) — the first real rel-v4 recoveries on the board, `candidates_with_recovery 2` ≤ 3, 0 bad frames, 0 fragments; rate report `959790d0…` = 3479.6 evals/h inclusive, nominal CoV 0.0175 over 61 clean, planning 3367.8 evals/h (`docs/l6_c2_session2_findings.md`). Owner's independent review and any `calibration.C2` pin pending; S and Claim B not opened; C1 #5 stays HOLD.** **OWNER 2026-09-03: C2 #2 = PASS adjudicated after independent re-check from the raw files (validator 66/66/67; 66 × 3 audit hashes; 64 map-guided genomes match; the two AUDIT-chunk CRC recoveries = allowed recoveries, not a loss, not a stop-loss event; rate report regenerated field-identical); `f38e263` pushed; `calibration.C2` PINNED = `959790d0…`. Both v0.6 calibrations pinned; the runner derives the S plan from the two planning rates = the owner's numbers (N 6061, 382 sampled audits, 112 575 expected inbound frames, CRC budget 451, timeout 8702 s, seed 1278628687, abba). S CONDITIONALLY open: its ruling pair is issued by the owner only after the C2-pin commit is pushed and binds that committed manifest sha256; no S ruling, no board before that; Claim B closed; C1 #5 stays HOLD.** **S ruling pair `2026-09-03-03` issued by the owner (bound to manifest `d39b339b…`; test-order fix `7f28cc1` pushed first). S #2 (rel-v4, v0.6, abba; runner-derived N 6061 / 382 audits / 112 575 frames / budget 451 / timeout 8702 s = the owner's numbers) = HOLD (2026-09-03 18:18): power cycle (UART 18:12), boundary PASS, IDENT 1.3.0 with both controls exact; 144 SCORED (1 + 143) in 78.9 s, 11/11 due sampled audits verified with zero retries; at go + 79.6 s the console lost ≈850 contiguous bytes across the tail of `HB 145 #12`, heartbeats #13–#15 and the head of `REC 145` — the merged line (the session's one bad frame, 0 fragments) was HB-shaped and malformed, not REC-shaped, so no RECGET; the collector's unchanged rule for a malformed non-transaction frame ended the epoch `CRASHED: unparseable frame` and the port closed before the board's rel-v4 REC resend could be seen (`docs/l6_s_session2_findings.md`). The `closing baseline scores` finding is a crash-path reporting artefact. Prereg-level observation: at the sampled-policy pace (143 intervals / 78.928 s = 6522.4 evals/h) a COMPLETED soak of N 6061 takes ≈3345 s ≈ 55.8 min < 0.9 T. Archived, NOT re-run. **OWNER 2026-09-03: §7 = the same way twice (S #1 + S #2: contiguous byte deletion on the same console path into a REC frame) — no third v0.6 soak; host-only batch authorised (malformed non-transaction line recorded once as BAD_FRAME and no longer handed to the collector; S #2 bytes replay + modelled byte-identical REC 145 resend with negatives; terminal bound on non-fatal bad frames in S; crash-path baseline gate on COMPLETED; the v0.7 heartbeat rule explicit; policy-matched N with wall time ≥ 0.9 T kept) → local commits → independent review → v0.7 re-freeze; no ruling, no board before; calibration.C1/C2 stay pinned; Claim B closed; C1 #5 stays HOLD.** **HOST BATCH AFTER S #2 (host-only, no image change) reviewed over three rounds — two HOLDs (three fail-closed gaps, then three interface gaps), both corrected — and PASSED 2026-09-03; pushed (`origin/main` = `6f5b5459`). Decisions accepted: D-b1 (a malformed non-transaction line is ledgered once, bounded globally by the D-s4 budget for EVERY shape, and no longer handed to the collector), D-h1 (the heartbeat rule budgets RECORDS: ≤ ⌊R/1000⌋ SCORED records may miss any heartbeat — 0 for a calibration, 12 for the soak), D-n1 (`policy_matched_wall` computed from the pinned calibrations' timing inputs, the FASTER arm sizing N and the slower one the timeout: N 12568, 789 audits, 233 364 frames, budget 934, timeout 8739 s; the post-hoc gate at S #2's pace predicts 6784.94 s ≥ the 6480 s floor), D-i1 (C1 #6 / C2 #2 imported into v0.7 by explicit declaration, no re-calibration). **v0.7 FROZEN 2026-09-03** (`docs/l6_soak_prereg.md`, sha `95d177a1…` after the owner's same-day correction family removed three v0.6 planning-rule residues; v0.6 `bfd69d10…` superseded in history; `sessions.S.n_rule = policy_matched_wall`; both imports declared; the bad-frame and heartbeat rules merged into `pass_conditions`). The image is unchanged and NOT rebuilt (`5deee74c…`). Next: the S ruling pair bound to the new committed manifest sha, a power cycle and a fresh boundary record — ONE soak under v0.7. C1 #5 and S #2 stay HOLD; Claim B stays closed until that soak completes and is independently adjudicated.** | `docs/l6_soak_prereg.md`, `docs/l6_instrument.md`, `manifests/l6_manifest.json` (frozen prereg, pinned image, pinned seeds/carrier) |

Board: EBAZ4203 `17A6`, U-Boot control plane, carrier `builds/p3/p3.bit` (`956379fa…`). Clocks board-confirmed 2026-09-01 (`evidence/preflight/slcr_17A6_2026-09-01-05.json`, read-only ruling `-05`): ARM PLL 1333.33 MHz, CPU_6x4x 666.67 MHz, **`CLK_621_TRUE` = 1 (6:2:1)**, PERIPHCLK 333.33 MHz — the last open clock observation of the L5 line, closed; it did not bear on the PASS.
Sessions: 2 L2 instrument outcomes + 1 PASS; L3 session #1 STOP (instrument) + diagnostic ×2
+ 5 PASS; L4 1 PASS; L5 3 HOLD (1 STOPPED, 1 CRASHED, 1 instrument-rejected) + 1 PASS (scoped). No kill criterion triggered; prereg §6's design-review trigger fired after session 3 and was discharged by rounds 1–4.

## Test-suite status and environment caveat

`host/run_tests.sh` runs the suite and lands an evidence report **fail-closed**
(`host/test_report.py`: atomic write → registration in `docs/import_manifest.md` → `git add`;
any failure → exit 3 regardless of the suite's status; the suite's own status is returned
only when the report landed). The report records `exit_status`, counts, `skipped`,
`head_at_run` (the HEAD when the suite ran — necessarily earlier than the commit that
includes the report; `worktree_dirty` says whether the tree differed), the `sudo -u
p3signer` probe and `boundary_available`.

`test_known_answer_through_the_real_signer` needs the D4 boundary to be usable; where sudo
is blocked (`no new privileges`, `/etc/sudo.conf` ownership) it **skips with the exact sudo
error as the reason** — a skip is counted, never green. Reviewer's sandbox 2026-08-31 saw
233/1 before this; after: 

| environment | report | ran | result | boundary_available | no_new_privs | exit |
|---|---|---|---|---|---|---|
| this host, user `test`, sudo works | `evidence/tests/test_report_2026-08-31T153501Z.json` | 240 | OK | true | false | 0 |
| same host under `setpriv --no-new-privs` (sudo refused exactly as in the sandbox) | `evidence/tests/test_report_2026-08-31T153507Z.json` | 240 | OK (skipped=1) | false | true | 0 |

Earlier reports in `evidence/tests/` (`152909Z`…`153424Z`) predate the fail-closed tool and
carry a `commit` field that is simply the HEAD at their run time; they are kept as evidence.
Fail-closed behaviour itself is unit-tested (read-only output dir, unwritable manifest,
non-repo → `ReportError` / exit 3).
